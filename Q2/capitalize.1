%{
#include <stdio.h>
%}

%%

abc         { printf("ABC"); }           /* Replace abc with ABC */
[a-zA-Z]+   { printf("%s ", yytext); }   /* Print English words */
[ \t\n]     { printf("%s", yytext); }    /* Preserve whitespace */
.           { printf("%s", yytext); }    /* Print other characters */

%%

int main() {
    yylex();
    return 0;
}

int yywrap() {
    return 1;
}
